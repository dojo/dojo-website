if(!dojo._hasResource["tests.NodeList-data"]){ //_hasResource checks added by build. Do not use _hasResource directly in your code.
dojo._hasResource["tests.NodeList-data"] = true;
dojo.provide("tests.NodeList-data");
if(dojo.isBrowser){
	doh.registerUrl("tests.NodeList-data", dojo.moduleUrl("tests", "NodeList-data.html"));
}

}
